version: "2"

linters:
  default: none
  enable:
    - errcheck
    - govet
    - ineffassign
    - misspell
    - staticcheck
    - unused

issues:
  exclusions:
    rules:
      - path: "_test\\.go"
        linters:
          - errcheck
