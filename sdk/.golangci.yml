version: "2"

run:
  allow-higher-go-version: true

linters:
  default: none
  enable:
    - errcheck
    - govet
    - ineffassign
    - misspell
    - staticcheck
    - unused

issues:
  exclude-rules:
    - path: "_test\\.go"
      linters:
        - errcheck
